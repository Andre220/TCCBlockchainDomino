using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum DataEvents
{
    ConnectionInfoRequest = 0,
    PlayRequest = 1,
    PlayerMove = 2,
    RequestPlayerState = 3,
    EndGame = 4,
}
